{
  "name": "multi2vec-node",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile-ts": "npx tsc",
    "build": "rimraf ./build && tsc",
    "start:dev": "npx nodemon",
    "start": "npm run import",
    "import": "npm run build && node build/index.js",
    "big-import": "npm run build && node build/index-big.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.5.7",
    "nodemon": "^3.0.1",
    "rimraf": "^5.0.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "weaviate-ts-client": "^1.5.0"
  }
}
